// @ts-ignore
import { ethers } from "hardhat"
import { Raffle } from '../typechain-types'

async function enterRaffle():Promise<void> {
enterRaffle()
    .then(() => process.exit(0))
    .catch((error) => {
        console.error(error)
        process.exit(1)
    })
